# รายงานการเชื่อมต่อ Sellsuki Design System MCP

## สรุปงาน
- งาน: เปลี่ยนการเชื่อมต่อ MCP ของ Sellsuki Design System ไปเป็น SSK V.3
- วันที่: 2026-04-28 14:25
- พื้นที่ทำงานหรือรีโป: `c:\Users\uSeR\Desktop\Sellsuki\AMS\sellsuki-ams-deployed`

## เวอร์ชัน Sellsuki Design System
- สถานะเวอร์ชัน: `inferred`
- เวอร์ชันที่ใช้: `SSK V.3 / DS3 MCP`
- หลักฐานจาก MCP:
  - ค่า config เดิมใน `C:\Users\uSeR\.codex\config.toml` ชี้ไปที่ MCP ตัวเก่า `https://sellsukidesignsystemv12-zsj5.vercel.app/api/mcp`
  - งานนี้อัปเดตให้ชี้ไปที่ endpoint ใหม่ `https://ds3-mcp.vercel.app/api/mcp`
  - ผู้ใช้ระบุแหล่งอ้างอิงของ DS3 เพิ่มเติมคือ
    - `https://github.com/BearyCenter/Design-system-SSK-3.git`
    - `npx -y @uxuissk/ds3-mcp`
    - `https://ds3-mcp.vercel.app/api/v1/*`
    - `https://ds3-mcp.vercel.app/api/v1/openapi.json`

## Components ที่เรียกใช้งาน
`ไม่มี`

## Tokens ที่เรียกใช้งาน
`ไม่มี`

## ข้อเสนอแนะหรือสิ่งที่ถูกปรับเปลี่ยน
- อัปเดต global MCP config จาก endpoint เดิมไปเป็น `https://ds3-mcp.vercel.app/api/mcp`
- การเปลี่ยนแปลงนี้เป็นการตั้งค่าระดับ Codex ไม่ได้แก้ component หรือ token ในแอป
- ต้องเริ่ม session Codex ใหม่หรือ reload client เพื่อให้ MCP server ตัวใหม่ถูกโหลดเข้าจริง
- หากต้องการให้ fallback เป็นแบบ local process ในอนาคต สามารถพิจารณาใช้ `npx -y @uxuissk/ds3-mcp` แทน remote URL ได้

## สถานะการส่งขึ้น GitHub
- วิธีที่ใช้: `markdown-commit`
- เป้าหมาย: `docs/reports/sellsuki-dss/2026-04-28-1425-connect-ds3-mcp.md`
- ผลลัพธ์: `ไม่สำเร็จ` ยังไม่ได้ commit/push ในรอบนี้
